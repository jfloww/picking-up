# Authentication To-Do

## Build Now

- [x] Frontend login and signup screens.
- [x] Frontend protected `/app` route.
- [x] Frontend HTTP-only JWT cookie storage through Next.js route handlers.
- [x] Django registration endpoint.
- [x] Django JWT token endpoint.
- [x] Django token refresh endpoint.
- [x] Django `/api/auth/me/` endpoint.
- [x] Django CORS setup for local frontend.
- [x] Basic backend auth tests.

## Defer Until Later

- [ ] Forgot password.
- [ ] Email verification.
- [ ] Google/OAuth login.
- [ ] Passkeys.
- [ ] Multi-factor authentication.
- [ ] User profile settings.
- [ ] Account deletion.
- [ ] Advanced session management.
- [x] Refresh-token cookie rotation from frontend route handler. (middleware.ts refreshes expired access tokens and stores the rotated refresh cookie)
- [x] Token blacklist on logout. (LogoutView blacklists the refresh token; covered by test_logout_blacklists_refresh_token)
- [ ] Production security hardening checklist.
- [ ] Replace landing-page task mock with a real task-UI screenshot (after the task workspace is built).
- [ ] Restyle the /app placeholder onto the shadcn token system.

## Current Auth Shape

```text
Browser
  |
  v
Next.js auth route handlers
  |
  v
Django REST API + Simple JWT
  |
  v
Django User model
```

The browser should not directly store JWTs in local storage. Next.js stores the Django JWTs in HTTP-only cookies.
